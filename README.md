# radio-host
This repository takes a Wikipedia page URL, extracts key content, converts it into a Hinglish conversation script using an OpenAI, and generates conversational audio using ElevenLabs TTS.
End-to-end flow: Wikipedia URL → Cleaned text → Hinglish script → Audio (MP3)

